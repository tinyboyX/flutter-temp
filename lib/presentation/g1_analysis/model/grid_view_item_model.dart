class GridViewItemModel {
  GridViewItemModel({this.title, this.data, required this.isIcon, this.isTransfer});

  final String? title;
  final String? data;
  final bool isIcon;
  final bool? isTransfer;
}
